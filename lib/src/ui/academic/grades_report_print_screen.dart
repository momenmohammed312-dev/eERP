import '../../imports/imports.dart';

class GradesReportPrintScreen extends StatelessWidget {
  const GradesReportPrintScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: AppEmptyState(
      title: 'كشف الدرجات', subtitle: 'نسخة الطباعة قيد التطوير.'));
  }
}