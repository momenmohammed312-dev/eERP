import '../../imports/imports.dart';

class FinancialReportPrintScreen extends StatelessWidget {
  const FinancialReportPrintScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: AppEmptyState(
      title: 'التقرير المالي', subtitle: 'نسخة الطباعة قيد التطوير.'));
  }
}