import '../../imports/imports.dart';

class NewInventoryItemScreen extends ConsumerWidget {
  const NewInventoryItemScreen({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return const AppEmptyState(title: 'إضافة صنف', subtitle: 'نموذج إضافة صنف قيد التطوير.');
  }
}